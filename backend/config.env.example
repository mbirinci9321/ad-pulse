# AD Pulse - Bağlantı Ayarları
# Bu dosyayı 'config.env' olarak kopyalayınız.

# LDAP Sunucu (IP veya Domain)
LDAP_SERVER=192.168.1.10

# Domain Adı
LDAP_DOMAIN=sirket.local

# Yönetici Kullanıcı (UPN formatı önerilir)
LDAP_USERNAME=administrator@sirket.local

# Şifre
LDAP_PASSWORD=Sifre123!

# Base DN (Aramaların başlayacağı kök dizin)
LDAP_BASE_DN=DC=sirket,DC=local

# Mock Modu (Gerçek AD için false yapın)
MOCK_MODE=false

# Uygulama Portu
PORT=8000
